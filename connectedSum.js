function connectedNodes(value) {
  this.values = {};
  this.numberOfValues = 0;

  this.addValue = function(value) {
    this.values[value] = true;
  };
}

function getConnectedSums(n, edges) {
  const addedNodes = {};
}
